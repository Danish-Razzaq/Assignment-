
function username() {
    let usernames = ['Aslam', 'Anwer', 'Danish', 'Eman', 'Rizwan', 'Amir', 'Hussain', 'Sana']

    usernames = []      //Remove name using arrary method and print msg

    if (usernames.length === 0) {
        console.log('We need to find some users!');

    }
}

username()